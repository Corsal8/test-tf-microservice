BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Code] ADD [isUsed] BIT NOT NULL CONSTRAINT [Code_isUsed_df] DEFAULT 0;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
